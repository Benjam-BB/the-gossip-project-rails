<html>
<body>
    
    
   
    <div class="row d-flex justify-content-center">
        <h1>Editer le Gossip n°<%= Gossip.find(params[:id]).id %>
        <div class="container d-flex justify-content-center">
            <%= form_tag url_for(action: 'update'), method: "PATCH" do %>
            <input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >        <br /> 
                <div><%= label_tag 'Edite le titre' %></div>
                <div><%= text_field_tag 'gossip_title', value = Gossip.find(params[:id]).title %></div>
                <br /> 
                <div><%= label_tag 'Edite le contenu' %></div>
                <div><%= text_field_tag 'gossip_content', value = Gossip.find(params[:id]).content%></div>
                <br /> 
                <%= submit_tag "Modifier", class: 'btn btn-primary btn-lg btn-submit'  %>
            <% end %>
            </form>
        </div>
    </div>
</body>
</html>
